# GeoPing
A distributed, multi-protocol (ICMP, TCP, UDP) ping utility written in Rust. Geoping uses a network of relay servers to measure latency from multiple endpoints simultaneously, allowing for rough geographical estimation of a target.
